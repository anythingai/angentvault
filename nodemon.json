{
  "watch": ["src/server"],
  "ext": "ts,json",
  "ignore": ["src/server/**/*.test.ts"],
  "exec": "ts-node --transpile-only --project tsconfig.server.json src/server/index.ts",
  "env": {
    "NODE_ENV": "development"
  }
} 